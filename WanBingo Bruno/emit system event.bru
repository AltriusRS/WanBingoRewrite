meta {
  name: emit system event
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/chat/system
  body: json
  auth: bearer
}

auth:bearer {
  token: test
}

body:json {
  {"message":"The WAN Show is scheduled to start in 20 minutes","username":"Hosts"}
}

settings {
  encodeUrl: true
}
