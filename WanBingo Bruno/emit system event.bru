meta {
  name: emit system event
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/chat/system
  body: json
  auth: bearer
}

auth:bearer {
  token: test
}

body:json {
  {"message":"Hello from curl","username":"tester"}
}

settings {
  encodeUrl: true
}
